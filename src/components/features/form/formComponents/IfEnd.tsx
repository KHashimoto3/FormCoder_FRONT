import { FormData } from "../../../types/formData";

export const IfEnd = () => {
  const preStyle = {
    fontSize: "16pt",
  };

  return (
    <>
      <pre style={preStyle}>{"}\n"}</pre>
    </>
  );
};
