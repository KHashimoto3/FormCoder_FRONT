import { Typography } from "@mui/material";

export const DashboardFooter = () => {
  return (
    <div style={{ background: "#ffffff", height: "auto" }}>
      <Typography variant="body1" sx={{ color: "#000000" }}>
        Form Coder <a href="/">システムについて</a>
      </Typography>
    </div>
  );
};
